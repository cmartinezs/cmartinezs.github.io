Continúa con el siguiente batch pendiente según `.raw/plan-marca-personal/00_estado_avance.md`.

Ejecuta solo ese batch, siguiendo su sección en `09_claude_code_batches.md`.

Marca el batch como `en_progreso` antes de tocar código y como `realizado` solo si queda completo y `npm run build` pasa.

Actualiza el historial en `00_estado_avance.md`.

No ejecutes más de un batch.
No hagas commit.